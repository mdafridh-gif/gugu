import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.afridh.myapplication',
  appName: 'My Application',
  webDir: 'dist'
};

export default config;
